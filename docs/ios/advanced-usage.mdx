---
sidebar_position: 5
---

# Advanced usage

## Turn on the setup / preview screen

The audio / video setup screen that appears for the participant right before
getting into the meeting is turned off by default. You can set `showSetupScreen`
in `meetingConfig` to `true` to turn it on (default value is `false`).

<Tabs
groupId="ios-language"
defaultValue="swift"
values={[
{ label: 'Swift', value: 'swift', },
{ label: 'Obj-C', value: 'obj-c', },
]
}>
<TabItem value="swift">

```swift {6}
import DyteSdk;

let  config = DyteMeetingConfig();
meetingConfig.roomName = "YOUR_ROOM_NAME"
meetingConfig.authToken = "YOUR_AUTH_TOKEN"
meetingConfig.showSetupScreen = true
```

</TabItem>
<TabItem value="obj-c">

```objectivec {6}
@import DyteSdk;

DyteMeetingConfig *meetingConfig = [[DyteMeetingConfig alloc] init];
meetingConfig.roomName = @"YOUR_ROOM_NAME";
meetingConfig.authToken = @"YOUR_AUTH_TOKEN";
meetingConfig.showSetupScreen = YES;
```

</TabItem>
</Tabs>

## Dynamic codec switching

Dyte uses VP9 codec by default, but switches to H264/VP8 depending on
participants' video codec support. VP9 is a superior codec but not all browsers
support it. If you set `autoTune` in `meetingConfig` to `false`, Dyte will use
VP8 only for all participants (default value is `true`).

<Tabs
groupId="ios-language"
defaultValue="swift"
values={[
{ label: 'Swift', value: 'swift', },
{ label: 'Obj-C', value: 'obj-c', },
]
}>
<TabItem value="swift">

```swift {6}
import DyteSdk;

let  config = DyteMeetingConfig();
meetingConfig.roomName = "YOUR_ROOM_NAME"
meetingConfig.authToken = "YOUR_AUTH_TOKEN"
meetingConfig.autoTune = true
```

</TabItem>
<TabItem value="obj-c">

```objectivec {6}
@import DyteSdk;

DyteMeetingConfig *meetingConfig = [[DyteMeetingConfig alloc] init];
meetingConfig.roomName = @"YOUR_ROOM_NAME";
meetingConfig.authToken = @"YOUR_AUTH_TOKEN";
meetingConfig.autoTune = YES;
```

</TabItem>
</Tabs>

## Control the meeting, and take meeting & participant actions

The `DyteMeetingView` instance (referenced as `dyteView`) helps you interact
with the meeting. You can take actions on the meeting as well as the
participants. You can also setup custom controls to that interact with the
meeting in certain ways that help you send events and messages across to other
participants in the meeting. You can find the complete reference to the
`DyteMeetingView` type [here](./reference/dyte-meeting). Read on to understand
how to use these individual controls.

<!-- ## Get info about meeting's connection configuration

You can get the connection configuration of the meeting in progress using the `config` property of `DyteMeeting`.

<Tabs
  groupId="ios-language"
  defaultValue="swift"
  values={[
    { label: 'Swift', value: 'swift', },
    { label: 'Obj-C', value: 'obj-c', },
  ]
}>
<TabItem value="swift">

```swift
let config = meeting.config
```

</TabItem>
<TabItem value="obj-c">

```objectivec
DyteMeetingConfig *config = meeting.config;
```

</TabItem>
</Tabs>

sets the value of `config` to an instance of [`DyteMeetingConfig`](./reference/connection-config). -->

## Replace the meeting control buttons with your own buttons

Let's say you don't want to give many options to the participants in a meeting,
other than the basic audio and video turning on, or you don't like our buttons
and control bar design (you really should give us feedback too, in this case),
or for any other reason - you just want to build your own buttons for basic
meeting controls. This is possible with Dyte, with the minor caveat that you
will need to put some validations at your end.

First thing in such a scenario would be to do away with our control bar

<Tabs
groupId="ios-language"
defaultValue="swift"
values={[
{ label: 'Swift', value: 'swift', },
{ label: 'Obj-C', value: 'obj-c', },
]
}>
<TabItem value="swift">

```swift
dyteView.updateUIConfig(["controlBar": false])
```

</TabItem>
<TabItem value="obj-c">

```objectivec
[dyteView updateUIConfig:@{ @"controlBar": false }];
```

</TabItem>
</Tabs>

Now you can perform the basic functions of a meeting, like below. These
functions can be used as the `IBAction` handlers of your own buttons.

### Mic control

To turn off the mic

<Tabs
groupId="ios-language"
defaultValue="swift"
values={[
{ label: 'Swift', value: 'swift', },
{ label: 'Obj-C', value: 'obj-c', },
]
}>
<TabItem value="swift">

```swift
DyteSelfParticipant.sharedInstance().disableAudio()
```

</TabItem>
<TabItem value="obj-c">

```objectivec
[[DyteSelfParticipant sharedInstance] disableAudio];
```

</TabItem>
</Tabs>

To turn on the mic

<Tabs
groupId="ios-language"
defaultValue="swift"
values={[
{ label: 'Swift', value: 'swift', },
{ label: 'Obj-C', value: 'obj-c', },
]
}>
<TabItem value="swift">

```swift
DyteSelfParticipant.sharedInstance().enableAudio()
```

</TabItem>
<TabItem value="obj-c">

```objectivec
[[DyteSelfParticipant sharedInstance] enableAudio];
```

</TabItem>
</Tabs>

### Camera control

To turn off the camera

<Tabs
groupId="ios-language"
defaultValue="swift"
values={[
{ label: 'Swift', value: 'swift', },
{ label: 'Obj-C', value: 'obj-c', },
]
}>
<TabItem value="swift">

```swift
DyteSelfParticipant.sharedInstance().disableVideo()
```

</TabItem>
<TabItem value="obj-c">

```objectivec
[[DyteSelfParticipant sharedInstance] disableVideo];
```

</TabItem>
</Tabs>

To turn on the camera

<Tabs
groupId="ios-language"
defaultValue="swift"
values={[
{ label: 'Swift', value: 'swift', },
{ label: 'Obj-C', value: 'obj-c', },
]
}>
<TabItem value="swift">

```swift
DyteSelfParticipant.sharedInstance().enableVideo()
```

</TabItem>
<TabItem value="obj-c">

```objectivec
[[DyteSelfParticipant sharedInstance] enableVideo];
```

</TabItem>
</Tabs>

<!-- ### Screenshare control

To turn screenshare on / off

<Tabs
  groupId="ios-language"
  defaultValue="swift"
  values={[
    { label: 'Swift', value: 'swift', },
    { label: 'Obj-C', value: 'obj-c', },
  ]
}>
<TabItem value="swift">

```swift
meeting.self.selfToggleScreenShare()
```

</TabItem>
<TabItem value="obj-c">

```objectivec
[meeting.self selfToggleScreenShare];
```

</TabItem>
</Tabs> -->

### Pinning

To pin your video to the meeting grid

<Tabs
groupId="ios-language"
defaultValue="swift"
values={[
{ label: 'Swift', value: 'swift', },
{ label: 'Obj-C', value: 'obj-c', },
]
}>
<TabItem value="swift">

```swift
DyteSelfParticipant.sharedInstance().selfPin()
```

</TabItem>
<TabItem value="obj-c">

```objectivec
[[DyteSelfParticipant sharedInstance] selfPin];
```

</TabItem>
</Tabs>

To unpin your video from the meeting grid

<Tabs
groupId="ios-language"
defaultValue="swift"
values={[
{ label: 'Swift', value: 'swift', },
{ label: 'Obj-C', value: 'obj-c', },
]
}>
<TabItem value="swift">

```swift
DyteSelfParticipant.sharedInstance().selfUnpin()
```

</TabItem>
<TabItem value="obj-c">

```objectivec
[[DyteSelfParticipant sharedInstance] selfUnpin];
```

</TabItem>
</Tabs>

### Leave meeting

To leave meeting

<Tabs
groupId="ios-language"
defaultValue="swift"
values={[
{ label: 'Swift', value: 'swift', },
{ label: 'Obj-C', value: 'obj-c', },
]
}>
<TabItem value="swift">

```swift
DyteSelfParticipant.sharedInstance().leaveRoom()
```

</TabItem>
<TabItem value="obj-c">

```objectivec
[[DyteSelfParticipant sharedInstance] leaveRoom];
```

</TabItem>
</Tabs>

## Get list of all participants in the meeting

You can get a list of all participants in the meeting using the `participants`
property of `DyteParticipants`.

<Tabs
groupId="ios-language"
defaultValue="swift"
values={[
{ label: 'Swift', value: 'swift', },
{ label: 'Obj-C', value: 'obj-c', },
]
}>
<TabItem value="swift">

```swift
let participants = DyteParticipants.sharedInstance().participants
```

</TabItem>
<TabItem value="obj-c">

```objectivec
NSArray *participants = [DyteParticipants sharedInstance].participants;
```

</TabItem>
</Tabs>

sets `participants` to an NSArray whose elements are instances of type
`DyteParticipant`, whose prototype is defined in the reference section. There
will be exactly one element of type `DyteSelfParticipant`, which refers to the
current participant.

## Controlling individual participants and actions

Dyte client SDK provide methods and properties to help you control participant
actions and certain UI elements. This could include actions that affect the
participant themselves as well as other participants (like a host with
privileged permissions may want to control the video of other participants).
This section defines all the possible properties and actions that can be taken.

:::info

Remember that despite the functions being available to every participant via the
client SDK, they will not take affect until coupled with the right permissions
for the participant who intends to use these.

:::

### Get participant list

To get a list of all participants, use
`DyteParticipants.sharedInstance.participants`: it returns `NSArray` where all
meeting participants are [`DyteParticipant`](./reference/participant) and the
current participant is [`DyteSelfParticipant`](./reference/self-participant).

### Participant info and actions

Once you get the array of participants as mentioned above, you can iterate over
that and get certain info about every participant.

We will consider the following iterator for the next section.

<Tabs
groupId="ios-language"
defaultValue="swift"
values={[
{ label: 'Swift', value: 'swift', },
{ label: 'Obj-C', value: 'obj-c', },
]
}>
<TabItem value="swift">

```swift
for p in DyteParticipants.sharedInstance().participants {
    // participant specific code goes here
}
```

</TabItem>
<TabItem value="obj-c">

```objectivec
for (DyteParticipant *p in [DyteParticipants sharedInstance].participants) {
    // participant specific code goes here
}
```

</TabItem>
</Tabs>

#### Peer ID

Get unique ID for every participant. This ID is generated by Dyte when a
participant joins the meeting. This ID is used for referring to a participant
within Dyte.

:::info

Even if the same participant, using the same auth token, rejoins the meeting,
for example by refreshing the page / app or due to connection drop, the peer ID
would be refreshed in that case.

:::

<Tabs
groupId="ios-language"
defaultValue="swift"
values={[
{ label: 'Swift', value: 'swift', },
{ label: 'Obj-C', value: 'obj-c', },
]
}>
<TabItem value="swift">

```swift
let peerID: String = p.id
```

</TabItem>
<TabItem value="obj-c">

```objectivec
NSString *peerID = p.id;
```

</TabItem>
</Tabs>

sets the value of `peerID` to a UUID string.

#### Client specific ID

Get the client specific ID as you have specified in the add participant API
call. You may want to decide exposing custom controls or build other business
logic based on this identifier, which helps you find the participant info in
your system.

<Tabs
groupId="ios-language"
defaultValue="swift"
values={[
{ label: 'Swift', value: 'swift', },
{ label: 'Obj-C', value: 'obj-c', },
]
}>
<TabItem value="swift">

```swift
let clientSpecificID: String = p.clientSpecificId
```

</TabItem>
<TabItem value="obj-c">

```objectivec
NSString *clientSpecificID = p.clientSpecificId;
```

</TabItem>
</Tabs>

#### Participant name

Get the participant name as you have specified in the add participant API call.
The participant may have changed this name, if enabled.

<Tabs
groupId="ios-language"
defaultValue="swift"
values={[
{ label: 'Swift', value: 'swift', },
{ label: 'Obj-C', value: 'obj-c', },
]
}>
<TabItem value="swift">

```swift
let name: String = p.name
```

</TabItem>
<TabItem value="obj-c">

```objectivec
NSString *name = p.name;
```

</TabItem>
</Tabs>

#### Participant thumbnail photo / avatar

Get the participant photo as you have specified in the add participant API call.

<Tabs
groupId="ios-language"
defaultValue="swift"
values={[
{ label: 'Swift', value: 'swift', },
{ label: 'Obj-C', value: 'obj-c', },
]
}>
<TabItem value="swift">

```swift
let picture: String = p.picture
```

</TabItem>
<TabItem value="obj-c">

```objectivec
NSString *picture = p.picture;
```

</TabItem>
</Tabs>

#### Check if participant's audio is on

To check whether a participant's audio is on (mic is turned on), you can use

<Tabs
groupId="ios-language"
defaultValue="swift"
values={[
{ label: 'Swift', value: 'swift', },
{ label: 'Obj-C', value: 'obj-c', },
]
}>
<TabItem value="swift">

```swift
let audio: Boolean = p.audioEnabled
```

</TabItem>
<TabItem value="obj-c">

```objectivec
BOOL audio = p.audioEnabled;
```

</TabItem>
</Tabs>

which sets the value of `audio` to a boolean specifying if the mic is on or not.

#### Check if participant's video is on

To check whether a participant's video is on (camera is turned on), you can use

<Tabs
groupId="ios-language"
defaultValue="swift"
values={[
{ label: 'Swift', value: 'swift', },
{ label: 'Obj-C', value: 'obj-c', },
]
}>
<TabItem value="swift">

```swift
let video: Boolean = p.videoEnabled
```

</TabItem>
<TabItem value="obj-c">

```objectivec
BOOL video = p.videoEnabled;
```

</TabItem>
</Tabs>

which sets the value of `video` to a boolean specifying if the camera is on or
not.

#### Check if the participant is pinned to the grid

To check whether a participant's video is pinned to the meeting grid; by the
host, by the user, by the preset, or by any other mechanism; you can use

<Tabs
groupId="ios-language"
defaultValue="swift"
values={[
{ label: 'Swift', value: 'swift', },
{ label: 'Obj-C', value: 'obj-c', },
]
}>
<TabItem value="swift">

```swift
let pinned: Boolean = p.isPinned
```

</TabItem>
<TabItem value="obj-c">

```objectivec
BOOL pinned = p.isPinned;
```

</TabItem>
</Tabs>

which sets the value of `pinned` to a boolean specifying whether the video is
pinned or not.

#### Check if the participant is sharing screen

To check whether a participant's screen is being shared, you can use

<Tabs
groupId="ios-language"
defaultValue="swift"
values={[
{ label: 'Swift', value: 'swift', },
{ label: 'Obj-C', value: 'obj-c', },
]
}>
<TabItem value="swift">

```swift
let screensharing: Boolean = p.isScreensharing
```

</TabItem>
<TabItem value="obj-c">

```objectivec
BOOL screensharing = p.isScreensharing;
```

</TabItem>
</Tabs>

which sets the value of `screensharing` to a boolean specifying whether the
participant's screen is being shared or not.
