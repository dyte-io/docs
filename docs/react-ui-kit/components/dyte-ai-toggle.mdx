# DyteAiToggle

A button which toggles visibility of AI view.

When clicked it emits a `dyteStateUpdate` event with the data:

```ts
{
  activeAI: boolean;
}
```

## Props

<PropsTable of="dyte-ai-toggle" />
