---
image: /static/ui-kit/1.x.x/components/dyte-participant-tile.svg
description: >-
  Learn how to use and customize the DyteParticipantTile component in Dyte's
  React UI Kit with our detailed documentation.
---

# DyteParticipantTile

import CodeRunner from '@site/src/components/CodeRunner/CodeRunner';

A component which plays a participants video and allows for placement
of components like `dyte-name-tag`, `dyte-audio-visualizer` or any other component.

<CodeRunner 
hide={[{start:1, end: 3}]}
minHeight='300px'
file={`
import { DyteParticipantTile, DyteNameTag, DyteAudioVisualizer } from "@dytesdk/react-ui-kit";
import { useDyteMeeting } from "@dytesdk/react-web-core";

export default function MyMeetingUI() {
  const { meeting } = useDyteMeeting();
  return (
    <DyteParticipantTile participant={meeting.self}>
      <DyteNameTag participant={meeting.self}>
        <DyteAudioVisualizer slot="start" />
      </DyteNameTag>
    </DyteParticipantTile>
  );
}
`} />

You can change the `nameTagPosition` to any of the supported values
and change the placement of audio-visualizer in name-tag as well.


<CodeRunner 
hide={[{start:1, end: 3}]}
additionalDecorators={[{
    className: "tooltip",
    elementAttributes: { "data-text": "Try editing this" },
    line: 7,
    startColumn: 63,
    endColumn: 94,
  }]}
minHeight='300px'
file={`
import { DyteParticipantTile, DyteNameTag, DyteAudioVisualizer } from "@dytesdk/react-ui-kit";
import { useDyteMeeting } from "@dytesdk/react-web-core";

export default function MyMeetingUI() {
  const { meeting } = useDyteMeeting();
  return (
    <DyteParticipantTile participant={meeting.self} nameTagPosition="bottom-center">
      <DyteNameTag participant={meeting.self}>
        <DyteAudioVisualizer slot="start" />
      </DyteNameTag>
    </DyteParticipantTile>
  );
}
`} />

It also has a few variants.

<CodeRunner 
hide={[{start:1, end: 3}]}
highlight={[{start:12, end: 12}]}
minHeight='300px'
file={`
import { DyteParticipantTile, DyteNameTag, DyteAudioVisualizer } from "@dytesdk/react-ui-kit";
import { useDyteMeeting } from "@dytesdk/react-web-core";

export default function MyMeetingUI() {
  const { meeting } = useDyteMeeting();

  return (
    <DyteParticipantTile
      participant={meeting.self}
      nameTagPosition="bottom-center" 
      variant="gradient"
    >
      <DyteNameTag participant={meeting.self}>
        <DyteAudioVisualizer slot="start" />
      </DyteNameTag>
    </DyteParticipantTile>
  );
}
`} />


## Props

<PropsTable of="dyte-participant-tile" />

<head>
  <title>React UI Kit DyteParticipantTile</title>
</head>
