# Settings

import { loadMeeting } from '/src/utils/meeting';

```jsx
<DyteSettings meeting={meeting} />
```

<div
  className="chat-preview relative"
  ref={() => loadMeeting('dyte-el')}
  style={{ maxWidth: '100%' }}
>
  <dyte-settings
    id="dyte-el"
    style={{ width: '100%', height: '100%' }}
    ref={(el) => {
      if (el) {
        el.states = {
          prefs: {
            muteNotificationSounds: true,
          },
        };
      }
    }}
  />
</div>
